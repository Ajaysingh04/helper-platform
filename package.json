{
  "name": "helper-platform",
  "version": "2.0.0",
  "description": "Fullstack On-Demand Home Services Platform & Super Admin Portal",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run server:dev\" \"npm run client\"",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "client": "npm start --prefix frontend",
    "server": "npm start --prefix backend",
    "build": "CI=false npm run build --prefix frontend && node -e \"const fs=require('fs'); fs.cpSync('frontend/build', 'build', {recursive:true, force:true});\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
